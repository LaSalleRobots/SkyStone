language: android
dist: trusty
android:
    components:
        - tools
        - platform-tools
        #- build-tools-29.0.2
        #- android-28
                
before_install:
    - chmod +x gradlew

script:
    - ./gradlew build connectedCheck --stacktrace