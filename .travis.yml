language: android
dist: trusty
android:
    components:
        - tools
        - platform-tools
        - build-tools-29.0.2
        - android-27
                
before_install:
    - yes | sdkmanager "platforms;android-27"
    - chmod +x gradlew

script:
    - ./gradlew build connectedCheck --stacktrace